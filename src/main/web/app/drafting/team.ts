import {Player} from "./player";

export interface Team {
    club: string;
    g: Player[];
    k: Player[];
    h: Player[];
    m: Player[];
    t: Player[];
    r: Player[];
    s: Player[];
    x: Player[];
}
